alias ..='cd .. && clear && ls'
alias ...='cd ../.. && clear && ls'
alias ....='cd ../../.. && clear && ls'
alias downloads='cd ~/Downloads && clear && ls'
alias dotfiles='cd ~/dotfiles && clear && ls'

ccd() {
    mkdir -p -- "${1:?No target specified}" &&
    cd -- "$1"
    clear
}

cdls() {
  cd "$1"
  clear
  ls
}

alias cd='cdls'

alias ga='git add .'
alias gap='git add -p'
alias gai='git add -i'
alias gc='git commit'
alias gcc='git checkout'
alias gp='git push'
alias gpp='git pull'
alias gs='git status'
alias gl='git log'
alias glg="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias glb="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset %n %n %b %n ' --abbrev-commit"
alias gb='git branch'
alias gac='ga && gc'
alias gitsub='git submodule update --init --recursive'

alias rmall='yes | rm -r'

alias tdl='todoist list'
alias tda='todoist add'
alias tds='todoist sync'
alias tdr='rmtodoist.sh'

gacp () {
  ga
  git commit
  gp
}

alias pdf='evince'
alias x='clear && ls'

alias pac='sudo pacman -Syu'
alias :q="exit"

alias bc='bc -l'

alias n='npm'
alias nr='npm run'
alias nis='npm install --save'
alias nisd='npm install --save-dev'

alias wordpress='cd ~/Local Sites/web-front-react-and-wordpress/app/public/wp-content/themes/react-and-wordpress'

alias vj='v ~/texts/vim-journal.md'

if [[ $OSTYPE == "darwin"* ]]
  then alias v="~/nvim-osx64/bin/nvim"
  alias run='npm run start:stage'
  
  else
  alias v="nvim"
fi

source="./lumalises"
